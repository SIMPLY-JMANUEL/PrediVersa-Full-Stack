

  command = "npx pnpm --filter frontend... run build"
  publish = "frontend/build"
  node_version = "20"

[build.environment]
NODE_VERSION = "16.20.2"
NODE_OPTIONS = "--openssl-legacy-provider"
  PNPM_HOME = "/opt/buildhome/.pnpm"
